name: Assign Issue to Copilot
# This workflow is triggered when an issue is labeled
on:
  issues:
    types:
      - labeled

# This workflow needs permission to modify issues
permissions:
  issues: write

# This workflow contains one job: assign_copilot_user
jobs:
  assign_copilot_user:
    name: Assign issue to copilot
    runs-on: ubuntu-latest
    if: github.event.label.name == 'copilot'
    steps:
      # This step assigns the issue to the copilot user
      - name: Assign issue to copilot
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const response = await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              assignees: ['copilot']
            });
            const assigned = response.data.assignees.some(a => a.login === 'copilot');
            if (!assigned) {
              core.setFailed('Failed to assign copilot to issue. Ensure GitHub Copilot coding agent is enabled for this repository.');
            }
